# 知问库 - 使用指南

## 项目简介

「知问库」是一款微信小程序，定位为 **个人知识管理 + 社区问答 + 笔试自测** 三位一体的学习工具。

---

## 目录结构

```
knowledge/
├── backend/          # Python 后端 (FastAPI)
│   ├── venv/         # Python 虚拟环境
│   ├── .env          # 配置文件 (微信密钥等)
│   ├── main.py       # 启动入口
│   └── app/          # 应用代码
└── mini/             # 微信小程序前端
    ├── app.js        # 小程序入口
    └── pages/        # 页面文件
```

---

## 第一步：启动后端服务

### 1.1 打开终端，进入后端目录

```powershell
cd e:\python_AI\实例工程仓库\vx小程序\knowledge\backend
```

### 1.2 启动服务器

```powershell
.\venv\Scripts\python.exe -m uvicorn main:app --host 127.0.0.1 --port 8000
```

看到以下输出说明成功：

```
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
INFO:     Application startup complete.
```

### 1.3 验证后端正常

浏览器打开 http://127.0.0.1:8000/docs 能看到 Swagger API 文档页面。

> 首次启动会自动下载中文向量模型（约 400MB），请耐心等待。
> 如果下载失败，代码已配置 hf-mirror.com 镜像，一般可自动解决。

---

## 第二步：打开小程序

### 2.1 下载安装微信开发者工具

下载地址：https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html

安装完成后用微信扫码登录。

### 2.2 导入项目

1. 打开微信开发者工具
2. 点击 **「+」导入项目**
3. **项目目录** 选择：`e:\python_AI\实例工程仓库\vx小程序\knowledge\mini`
4. **AppID** 选择 **测试号**（如果没有真实 AppID）
5. 点击 **确定**

### 2.3 配置后端地址（重要）

打开 `mini/app.js`，确认 `baseUrl` 指向你的后端：

```javascript
globalData: {
    baseUrl: 'http://127.0.0.1:8000/api'  // 本机开发用这个
}
```

> 如果要在手机上预览，需要将 `127.0.0.1` 改为你电脑的局域网 IP（如 `192.168.1.100`）。

### 2.4 关闭域名校验（开发阶段）

微信开发者工具 → 右上角 **详情** → **本地设置** → 勾选：

- [x] 不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书

### 2.5 预览小程序

点击开发者工具顶部的 **预览** 或 **编译** 按钮，即可在模拟器中看到小程序。

---

## 第三步：功能使用说明

### 3.1 社区圈（首页 Tab）

- 浏览所有人发布的题目帖
- 支持按 **最新 / 最热 / 未解决** 排序
- 可 **点赞** 和 **收藏** 题目到个人题库
- 点击帖子可查看评论、发表评论

### 3.2 我的题库（第二个 Tab）

- 查看个人所有题目
- 顶部搜索框支持 **关键词搜索** 和 **语义搜索**
- 点击 **筛选** 可按技术栈、难度、掌握程度过滤
- 点击题目右侧的掌握标签可切换状态：`未掌握 → 学习中 → 已掌握`
- 标记为「已掌握」的题目自动进入归档区
- 点击 **查看归档** 可看到已掌握的题目

### 3.3 发布题目（第三个 Tab）

- 输入标题和内容发布题目帖
- 选择技术栈和难度
- 可添加自定义标签
- 勾选「同时存入我的题库」则发帖同时入库

### 3.4 我的（第四个 Tab）

- 查看个人头像、昵称、声望值
- 题库统计概览：总题数、已掌握数、累计录入数
- 掌握进度条
- 技术栈分布

---

## 第四步：配置微信真实登录（可选）

开发阶段默认使用 mock 登录（code 直接作为 openid），无需配置即可使用。

如果要接入真实微信登录：

### 4.1 获取 AppID

1. 登录 https://mp.weixin.qq.com
2. 注册一个小程序账号（个人即可）
3. 在 **开发管理 → 开发设置** 中获取 **AppID** 和 **AppSecret**

### 4.2 修改配置

编辑 `backend/.env`：

```
WECHAT_APP_ID=你的AppID
WECHAT_APP_SECRET=你的AppSecret
```

### 4.3 重启后端

```powershell
# 先 Ctrl+C 停止后端
# 再重新启动
.\venv\Scripts\python.exe -m uvicorn main:app --host 127.0.0.1 --port 8000
```

---

## 第五步：API 接口速查

后端启动后访问 http://127.0.0.1:8000/docs 可查看完整交互式文档。

主要接口：

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/login` | 微信登录，返回 JWT token |
| GET | `/api/auth/me` | 获取当前用户信息 |
| GET | `/api/questions/` | 获取题库列表（支持筛选） |
| POST | `/api/questions/` | 手动录入题目 |
| GET | `/api/questions/{id}` | 获取题目详情 |
| PUT | `/api/questions/{id}` | 更新题目 |
| DELETE | `/api/questions/{id}` | 删除题目 |
| POST | `/api/questions/{id}/mastery?mastery=0|1|2` | 设置掌握程度 |
| POST | `/api/questions/batch-mastery?mastery=0|1|2` | 批量设置掌握程度 |
| POST | `/api/questions/upload` | 上传文件解析 QA |
| POST | `/api/questions/import` | 批量导入解析结果 |
| POST | `/api/questions/collect/{post_id}` | 从社区收藏题目 |
| GET | `/api/questions/stats/overview` | 题库统计 |
| GET | `/api/posts/` | 获取帖子列表 |
| POST | `/api/posts/` | 发布帖子 |
| GET | `/api/posts/{id}` | 帖子详情 |
| POST | `/api/posts/{id}/like` | 点赞 |
| GET | `/api/posts/{id}/comments` | 获取评论 |
| POST | `/api/posts/{id}/comments` | 发表评论 |
| POST | `/api/posts/{id}/comments/{cid}/adopt` | 采纳答案 |

---

## 常见问题

### Q: 后端启动报错 "向量模型加载失败"
A: 网络问题导致模型下载失败。已配置 hf-mirror.com 镜像，如果仍然失败，可以手动下载模型放到本地缓存目录，或忽略此错误——向量搜索不可用时，其他功能（CRUD、社区）均正常工作。

### Q: 小程序预览白屏
A: 确认后端已启动；确认 `app.js` 中 `baseUrl` 地址正确；确认已关闭域名校验。

### Q: 手机上预览无法请求后端
A: 将 `baseUrl` 从 `127.0.0.1` 改为电脑的局域网 IP。确保手机和电脑在同一 WiFi 下。

### Q: 数据库文件在哪？
A: `backend/data/knowledge.db`（SQLite 文件），可直接用 DB Browser for SQLite 查看。

### Q: 如何切换到 MySQL？
A: 修改 `backend/.env` 中的 `DATABASE_URL`：
```
DATABASE_URL=mysql+pymysql://user:password@localhost:3306/knowledge
```
并安装 `pymysql`：`.\venv\Scripts\pip.exe install pymysql`

---

## 技术栈说明

| 组件 | 选型 | 说明 |
|------|------|------|
| 前端 | 微信原生小程序 | 无框架，最简单 |
| 后端 | Python + FastAPI | 自动生成 API 文档 |
| 数据库 | SQLite | 零配置，开发即用 |
| 向量库 | FAISS | Facebook 开源，pip 安装 |
| 向量模型 | text2vec-base-chinese | 中文语义搜索 |
| 认证 | JWT | 微信 code 换 token |
